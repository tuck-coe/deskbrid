name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        target:
          - target: x86_64-unknown-linux-gnu
        os:
          - ubuntu-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          target: ${{ matrix.target }}

      - name: Install cross compiler (aarch64)
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: sudo apt-get update -qq && sudo apt-get install -yqq gcc-aarch64-linux-gnu libssl-dev:arm64 || sudo apt-get install -yqq gcc-aarch64-linux-gnu

      - name: Install OpenSSL (x86_64)
        if: matrix.target == 'x86_64-unknown-linux-gnu'
        run: sudo apt-get update -qq && sudo apt-get install -yqq libssl-dev

      - run: cargo build --release --target ${{ matrix.target }}

      - name: Package tarball
        run: |
          cd target/${{ matrix.target }}/release
          tar czf "deskbrid-${{ matrix.target }}.tar.gz" deskbrid

      - name: Upload tarball to release
        uses: softprops/action-gh-release@v2
        with:
          files: target/${{ matrix.target }}/release/deskbrid-*.tar.gz
          generate_release_notes: true
