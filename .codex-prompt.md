Add the following to the deskbrid project at /home/coemedia/projects/deskbrid. Read all existing src/*.rs files first to understand the current code.

1. **display:list action** — Add to dispatch_action in src/lib.rs. Query Mutter.DisplayConfig via DBus to get monitor info (width, height, scale, refresh). Return format: {"monitors": [{"id": 0, "width": 1920, "height": 1080, "scale": 1.0, "refresh": 60}]}

2. **Graceful shutdown** — In src/main.rs, add a tokio::signal::ctrl_c() handler. On shutdown: remove the Unix socket file, log "shutting down", and exit cleanly. Pass a shutdown signal through the daemon so background tasks stop gracefully too.

3. **Startup resilience** — In src/lib.rs run() function, each subsystem initialization (InputSession, Hub, clipboard Monitor) should use warn-and-continue pattern if it fails. Store what's available and report capabilities accordingly. The daemon should never crash on startup — if RemoteDesktop isn't available, log a warning and skip input injection capabilities.

4. **Systemd user service file** — Create deploy/deskbrid.service with:
```
[Unit]
Description=Deskbrid desktop agent HAL
After=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.cargo/bin/deskbrid
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
```

5. **Update README.md** — Add a "Prerequisites" section listing: wl-clipboard (clipboard), gnome-screenshot or grim (screenshots), systemd (user service).

Build rules:
- cargo check must pass
- No unwrap() in production paths
- Use tracing::warn for degraded capabilities
- Keep PROTOCOL.md in sync if you change action signatures
